import { Metadata } from "next"

export default async function Barbeiros(){
    return (
        <>
            barbeiros
        </>
    )
}

export const metadata: Metadata = {
    title: 'Barbearias RL',
}